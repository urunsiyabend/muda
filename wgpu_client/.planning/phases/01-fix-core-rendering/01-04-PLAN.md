---
phase: 01-fix-core-rendering
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/ui/editor_tabs.rs
  - src/ui/file_tree.rs
autonomous: false

must_haves:
  truths:
    - "Tab bar items are clickable with proper hit detection"
    - "Sidebar items have correct hover and selection states"
    - "Visual appearance matches Zed/Linear aesthetic"
  artifacts:
    - path: "src/ui/editor_tabs.rs"
      provides: "Polished tab bar with visible text"
    - path: "src/ui/file_tree.rs"
      provides: "Polished file tree with visible text"
  key_links:
    - from: "src/ui/editor_tabs.rs"
      to: "GpuRenderer"
      via: "build_texts() and build_rects()"
---

<objective>
Verify and polish the visual appearance of tabs and sidebar after the text rendering fix.

Purpose: With text rendering fixed (Plan 01), verify that tab bar file names are visible and clickable. Apply visual polish to match the Zed/Linear aesthetic per CONTEXT.md. This plan includes a verification checkpoint.

Output: Polished, functional tabs and sidebar with confirmed visual correctness.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-core-rendering/01-CONTEXT.md
@.planning/phases/01-fix-core-rendering/01-01-SUMMARY.md

From CONTEXT.md:
- Visual style should feel like Zed or Linear - clean, minimal, lots of whitespace
- Dark mode focus for now
- Full visual polish: consistent spacing/alignment, clean visual hierarchy
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish tab bar visual design</name>
  <files>src/ui/editor_tabs.rs</files>
  <action>
    Review and polish the EditorTabs component for modern aesthetics:

    1. Verify text visibility:
       - Check that build_texts() produces correct TextBlock entries
       - Ensure font size (13.0) is readable
       - Verify text color contrast against background

    2. Adjust spacing for cleaner look:
       - TAB_PADDING_H: Consider 12-16px for comfortable spacing
       - TAB_GAP: 0-1px for minimal separation (Zed-like)
       - HEIGHT: 32-36px for comfortable touch/click targets

    3. Polish active tab indicator:
       - Active tab should have subtle background difference
       - 2px accent line at bottom (already implemented)
       - Consider subtle shadow or elevation for active tab

    4. Polish hover states:
       - Subtle background change on hover
       - Close button appears on hover
       - Smooth transition if animations are supported

    5. Verify hit detection:
       - on_click() correctly identifies clicked tab
       - Close button hit area is sufficient (16x16px minimum)
       - Middle-click to close works

    Keep changes minimal - the component structure is good, just verify visual polish.
  </action>
  <verify>
    cargo build --package wgpu_client
  </verify>
  <done>
    Tab bar has clean, modern appearance matching Zed/Linear aesthetic.
    All tabs have visible, readable file names.
    Click detection works correctly for tab selection and close.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish file tree visual design</name>
  <files>src/ui/file_tree.rs</files>
  <action>
    Review and polish the FileTree component for modern aesthetics:

    1. Verify text visibility:
       - Check build_texts() produces correct entries
       - File names are readable with good contrast
       - Directory vs file distinction is clear

    2. Adjust spacing:
       - Row height for comfortable reading (24-28px)
       - Left padding for hierarchy indication
       - Icon-to-text spacing

    3. Polish selection states:
       - Selected item has clear background highlight
       - Focused vs unfocused selection distinction
       - Hover state is visible but subtle

    4. Verify icon display:
       - Folder icons visible
       - File icons visible
       - Expand/collapse chevrons work

    5. Ensure scroll behavior:
       - Content clips at sidebar boundary (Plan 02)
       - Selection follows keyboard navigation

    Keep changes minimal - focus on verifying visibility and polish.
  </action>
  <verify>
    cargo build --package wgpu_client
  </verify>
  <done>
    File tree has clean, modern appearance.
    All file names are visible and readable.
    Selection and hover states are clear.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete rendering fix for the IDE:
    - Text rendering consolidated (no more disappearing text)
    - Scissor clipping added (no more overflow)
    - Debug overlay available (F12 to toggle)
    - Visual polish applied to tabs and sidebar
  </what-built>
  <how-to-verify>
    1. Run the application:
       ```
       cargo run --package wgpu_client
       ```

    2. Verify sidebar (FIX-01):
       - [ ] Click on sidebar items multiple times
       - [ ] Text should NOT disappear
       - [ ] Selection highlight should work

    3. Verify tab bar (FIX-02, FIX-03):
       - [ ] File names are visible and readable
       - [ ] Click on tabs to switch
       - [ ] Close button works
       - [ ] Hover state is visible

    4. Verify text area clipping (FIX-04):
       - [ ] Scroll down in a file
       - [ ] Text should NOT overflow into tab bar
       - [ ] Toggle debug overlay (F12) to see component bounds

    5. Verify overall visual quality:
       - [ ] Dark theme looks clean and modern
       - [ ] Spacing feels comfortable
       - [ ] No visual glitches or artifacts

    6. Test resize:
       - [ ] Resize window - no crashes
       - [ ] Layout adapts correctly
       - [ ] All text remains visible
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
This plan includes a human verification checkpoint that covers all Phase 1 requirements:

| Requirement | Verification |
|-------------|--------------|
| FIX-01: Sidebar text persists | Click sidebar items, no disappearance |
| FIX-02: Tab names visible | File names readable in tab bar |
| FIX-03: Tab click detection | Clicking tabs switches active tab |
| FIX-04: No text overflow | Text stays within bounds, use debug overlay |
</verification>

<success_criteria>
- Human verification confirms all Phase 1 requirements are met
- Visual appearance is clean and professional
- No regressions from the rendering fixes
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-core-rendering/01-04-SUMMARY.md`
</output>
