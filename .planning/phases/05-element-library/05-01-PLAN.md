---
phase: 05-element-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ora/src/style/units.rs
  - ora/src/style/mod.rs
  - ora/src/elements/div.rs
  - ora/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Unit functions px() and pct() construct Length values"
    - "Builder methods accept impl Into<Length> for flexibility"
    - "Div.row() and Div.column() set flex direction"
    - "JustifyContent and AlignItems enums accessible via justify() and items()"
  artifacts:
    - path: "ora/src/style/units.rs"
      provides: "Unit constructor functions"
      contains: "pub fn px"
    - path: "ora/src/elements/div.rs"
      provides: "Enhanced builder methods"
      contains: "pub fn justify"
  key_links:
    - from: "ora/src/style/units.rs"
      to: "ora/src/elements/div.rs"
      via: "Into<Length> conversions"
      pattern: "impl Into<Length>"
---

<objective>
Add explicit unit functions (px, pct) and enhance Div builder API with CSS-style methods.

Purpose: Establish the Tailwind-style fluent builder pattern foundation that all elements will use.
Output: Unit functions and enhanced Div builder with justify()/items()/row()/column() methods.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-element-library/05-CONTEXT.md
@.planning/phases/05-element-library/05-RESEARCH.md
@ora/src/style/units.rs
@ora/src/style/mod.rs
@ora/src/elements/div.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit constructor functions and Into conversions</name>
  <files>ora/src/style/units.rs, ora/src/style/mod.rs</files>
  <action>
Add unit constructor functions to units.rs:

1. Add `pub fn px(value: f32) -> Length { Length::Px(value) }`
2. Add `pub fn pct(value: f32) -> Length { Length::Percent(value) }`

3. Implement From conversions for ergonomics:
   - `impl From<f32> for Length` -> `Length::Px(value)` (raw numbers default to pixels)
   - `impl From<i32> for Length` -> `Length::Px(value as f32)` (integers work too)

4. In mod.rs, re-export the new functions:
   - `pub use units::{px, pct};`

The goal is to enable both explicit (`px(200)`) and implicit (`200.0`) unit specification.
  </action>
  <verify>Run `cargo build -p ora` - no compilation errors</verify>
  <done>px() and pct() functions exist, From conversions implemented, re-exported from style mod</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Div builder with CSS-style alignment methods</name>
  <files>ora/src/elements/div.rs</files>
  <action>
Add new builder methods to Div that accept enums directly (CSS flexbox style):

1. Replace sizing methods to accept `impl Into<Length>`:
   - `pub fn w(mut self, w: impl Into<Length>) -> Self`
   - `pub fn h(mut self, h: impl Into<Length>) -> Self`
   - `pub fn min_w(mut self, w: impl Into<Length>) -> Self`
   - `pub fn min_h(mut self, h: impl Into<Length>) -> Self`
   - `pub fn max_w(mut self, w: impl Into<Length>) -> Self`
   - `pub fn max_h(mut self, h: impl Into<Length>) -> Self`
   - Remove `w_pct` and `h_pct` (now redundant with `w(pct(50))`)

2. Add CSS-style alignment methods (keep existing methods for compatibility):
   - `pub fn row(mut self) -> Self` - sets flex_direction to Row (alias for flex_row)
   - `pub fn column(mut self) -> Self` - sets flex_direction to Column (alias for flex_col)
   - `pub fn justify(mut self, j: JustifyContent) -> Self` - sets justify_content
   - `pub fn items(mut self, a: AlignItems) -> Self` - sets align_items

3. Keep existing justify_center/justify_end/justify_between/align_center/align_end/align_stretch as convenience shortcuts.

4. Ensure gap() still accepts f32 directly (will add design token version in Phase 6).
  </action>
  <verify>Run `cargo build -p ora` - no compilation errors</verify>
  <done>Div builder accepts impl Into<Length>, has justify() and items() methods</done>
</task>

<task type="auto">
  <name>Task 3: Update lib.rs exports and verify compilation</name>
  <files>ora/src/lib.rs</files>
  <action>
1. Add re-exports for unit functions in lib.rs:
   - Add `pub use style::{px, pct};`

2. Add re-exports for alignment enums (if not already exported):
   - Add `pub use style::{JustifyContent, AlignItems};`

3. Run full compilation with all examples to verify:
   - `cargo build -p ora`
   - `cargo build -p ora --examples`
   - Ensure no breaking changes to existing examples
  </action>
  <verify>
Run `cargo build -p ora --examples` successfully
Run `cargo test -p ora` - all tests pass
  </verify>
  <done>px(), pct(), JustifyContent, AlignItems exported from ora crate, all examples compile</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cargo build -p ora` compiles without errors
2. `cargo test -p ora` passes all existing tests
3. `cargo build -p ora --examples` compiles all examples
4. Unit functions work: `px(200)`, `pct(50)`, `200.0.into()`
5. Div builder works: `Div::new().w(px(200)).justify(JustifyContent::Center).items(AlignItems::Start)`
</verification>

<success_criteria>
- px() and pct() functions create Length values
- Div sizing methods accept impl Into<Length>
- justify() and items() methods work with enum values
- All existing tests and examples continue to compile
</success_criteria>

<output>
After completion, create `.planning/phases/05-element-library/05-01-SUMMARY.md`
</output>
