---
phase: 13.1-rendering-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ora/src/rendering/text.rs
  - ora/src/element/trait_def.rs
  - ora/src/platform/gpu.rs
  - ora/src/platform/event_loop.rs
autonomous: true

must_haves:
  truths:
    - "Scrolling through a file reuses glyphon buffers for unchanged lines"
    - "Identical lines across buffers share shaped glyphs from a global content cache"
    - "Glyph cache hit rate is visible in --show-fps title bar"
    - "--no-glyph-cache flag disables the cache for A/B comparison"
  artifacts:
    - path: "ora/src/rendering/text.rs"
      provides: "GlyphCacheKey, LRU glyph cache, measure_text_cached(), return_buffers_to_cache()"
      contains: "LruCache"
    - path: "ora/src/element/trait_def.rs"
      provides: "PaintCommand::Text with optional GlyphCacheKey for buffer return"
      contains: "GlyphCacheKey"
    - path: "ora/src/platform/gpu.rs"
      provides: "render_frame returns Vec<(GlyphCacheKey, Buffer)> for cache reuse"
    - path: "ora/src/platform/event_loop.rs"
      provides: "Glyph hit rate in --show-fps, --no-glyph-cache CLI flag, buffer return after render_frame"
  key_links:
    - from: "ora/src/rendering/text.rs"
      to: "ora/src/element/trait_def.rs"
      via: "GlyphCacheKey carried in PaintCommand::Text"
      pattern: "GlyphCacheKey"
    - from: "ora/src/platform/gpu.rs"
      to: "ora/src/rendering/text.rs"
      via: "returned buffers fed back into cache"
      pattern: "return_buffers_to_cache"
---

<objective>
Add a global LRU glyph buffer cache to TextSystem so that identical text lines are shaped once and reused across frames and buffers.

Purpose: Glyphon set_text() + shape_until_scroll() is the single most expensive per-frame operation. Caching shaped Buffers keyed on (text_hash, font_size, line_height) eliminates redundant shaping for unchanged lines.

Output: TextSystem with LRU cache, cache-aware measure_text_cached(), buffer return path from GPU render, glyph hit rate in --show-fps, --no-glyph-cache debug flag.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-rendering-performance-optimization/13.1-CONTEXT.md
@.planning/phases/13.1-rendering-performance-optimization/13.1-RESEARCH.md
@ora/src/rendering/text.rs
@ora/src/rendering/texture.rs
@ora/src/element/trait_def.rs
@ora/src/platform/gpu.rs
@ora/src/platform/event_loop.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glyph buffer LRU cache in TextSystem</name>
  <files>ora/src/rendering/text.rs, ora/src/element/trait_def.rs</files>
  <action>
  1. In text.rs, add `GlyphCacheKey` struct:
     ```rust
     #[derive(Clone, Copy, PartialEq, Eq, Hash)]
     pub struct GlyphCacheKey {
         pub text_hash: u64,
         pub font_size_bits: u32,
         pub line_height_bits: u32,
     }
     ```
     Use `std::hash::{Hash, Hasher}` with `DefaultHasher` to hash text content into u64. Convert f32 to u32 bits via `f32::to_bits()`.

  2. Add to TextSystem struct:
     - `glyph_cache: LruCache<GlyphCacheKey, glyphon::Buffer>` (capacity 2048)
     - `cache_hits: u64`
     - `cache_misses: u64`
     - `cache_enabled: bool` (default true, for --no-glyph-cache)
     Import `use lru::LruCache;` and `use std::num::NonZeroUsize;` (same pattern as texture.rs).

  3. Add `measure_text_cached(&mut self, text: &str, font_size: f32, line_height: f32, max_width: Option<f32>) -> (GlyphCacheKey, Buffer, Size<f32>)`:
     - Compute GlyphCacheKey from text hash + font_size.to_bits() + line_height.to_bits()
     - If cache_enabled and key exists in cache: pop the Buffer out of the cache (LruCache::pop), measure its layout_runs for size, increment cache_hits, return (key, buffer, size)
     - Otherwise: call existing measure_text() logic, increment cache_misses, return (key, buffer, size)
     - IMPORTANT: We pop (remove) from cache because Buffer is moved into PaintCommand::Text. It gets returned after render_frame().

  4. Add `return_buffers_to_cache(&mut self, buffers: Vec<(GlyphCacheKey, glyphon::Buffer)>)`:
     - For each (key, buffer), call `self.glyph_cache.put(key, buffer)` to return it to the LRU cache.
     - Only if cache_enabled.

  5. Add `cache_hit_rate(&self) -> f64`:
     - Return hits / (hits + misses) as f64, or 0.0 if no calls yet.

  6. Add `set_cache_enabled(&mut self, enabled: bool)` and `reset_cache_stats(&mut self)`.

  7. In trait_def.rs, extend `PaintCommand::Text` to carry `Option<GlyphCacheKey>`:
     ```rust
     Text {
         buffer: glyphon::Buffer,
         left: f32,
         top: f32,
         bounds: Rect,
         color: Color,
         cache_key: Option<GlyphCacheKey>,
     }
     ```
     Update `PaintContext::paint_text()` to accept an optional GlyphCacheKey and pass it through. Add a convenience method `paint_text_cached()` that takes a GlyphCacheKey. The existing `paint_text()` should pass `cache_key: None` for backward compatibility.

  Do NOT change any call sites that use measure_text() yet -- they will be migrated incrementally. The existing measure_text() remains as-is.
  </action>
  <verify>
  `cargo check` passes with no errors. The new types and methods compile. Existing code is unaffected (backward compat via cache_key: None).
  </verify>
  <done>
  TextSystem has LruCache<GlyphCacheKey, Buffer> with capacity 2048. measure_text_cached() computes hash key, checks cache, falls back to shaping. PaintCommand::Text carries optional GlyphCacheKey. return_buffers_to_cache() accepts returned buffers. cache_hit_rate() reports stats.
  </done>
</task>

<task type="auto">
  <name>Task 2: Buffer return path from render_frame + --show-fps glyph stats + --no-glyph-cache flag</name>
  <files>ora/src/platform/gpu.rs, ora/src/platform/event_loop.rs</files>
  <action>
  1. In gpu.rs, modify `render_frame()` to collect (GlyphCacheKey, Buffer) pairs from PaintCommand::Text variants that carry a cache_key:
     - When processing PaintCommand::Text with `cache_key: Some(key)`, after adding the buffer to text_system via add_text_area_for_layer (or add_text_area), store the key in a local Vec.
     - CHALLENGE: The Buffer is moved into TextEntry inside TextSystem. The buffers need to be recovered AFTER text rendering is complete.
     - SOLUTION: Add a method `TextSystem::take_pending_cache_keys(&mut self) -> Vec<GlyphCacheKey>` that returns the keys collected during this frame. After render_frame completes, in event_loop.rs, call `text_system.drain_rendered_buffers()` which returns `Vec<(GlyphCacheKey, Buffer)>` by draining the pending_buffers/pending_layers and pairing with stored keys.
     - Actually simpler approach: In TextSystem, add `pending_cache_keys: Vec<Option<GlyphCacheKey>>` alongside pending_buffers. When add_text_area() is called, also push the cache_key. After prepare()+render(), the buffers in pending_buffers are consumed by glyphon. So instead: store keys, and after prepare/render, those Buffers are already consumed. We need to RE-shape on next frame if not cached.
     - REVISED approach (matching RESEARCH.md): Store cache_key alongside each TextEntry. After render_frame() calls text_system.prepare() and text_system.render(), call `text_system.drain_buffers_for_cache() -> Vec<(GlyphCacheKey, Buffer)>` which iterates pending_buffers, extracts entries with cache_keys, and returns (key, buffer) pairs. The Buffer is still valid after prepare/render -- glyphon TextRenderer only reads the Buffer during prepare/render, it does not consume or invalidate it. Then clear pending_buffers.

  2. Extend TextEntry to carry `cache_key: Option<GlyphCacheKey>`.

  3. Update `add_text_area()` and `add_text_area_for_layer()` to accept optional GlyphCacheKey parameter. The existing call sites pass None.

  4. Add `drain_buffers_for_cache(&mut self) -> Vec<(GlyphCacheKey, glyphon::Buffer)>`:
     - Iterate pending_buffers (and pending_layers), extract (key, buffer) where key is Some.
     - Clear pending_buffers/pending_layers as before (they were already cleared in existing prepare/render flow -- check current code).
     - Actually: check the existing clear pattern. If prepare() drains pending_buffers, then we need to collect BEFORE prepare. Adjust the approach based on actual code flow.

  5. In event_loop.rs, after `render_frame()` returns Ok:
     - Call `gpu_state.text_system.drain_buffers_for_cache()` to get returned buffers.
     - Call `gpu_state.text_system.return_buffers_to_cache(returned)` to put them back.

  6. In event_loop.rs, add `--no-glyph-cache` CLI flag:
     - Add a new `static NO_GLYPH_CACHE: AtomicBool` similar to SHOW_FPS.
     - Parse from `std::env::args()` in the init function (same place as --show-fps).
     - After TextSystem creation, if flag is set, call `text_system.set_cache_enabled(false)`.

  7. Extend the --show-fps title bar format to include glyph cache hit rate:
     ```
     "Muda [{total}ms | view {v} layout {l} prepaint {p} paint {pa} gpu {g} | glyph {rate}% | {cmds} cmds | {fps}fps]"
     ```
     Call `gpu_state.text_system.cache_hit_rate()` to get the rate.

  IMPORTANT: Study the existing prepare()/render()/clear flow in text.rs carefully before implementing drain_buffers_for_cache. The key constraint is: Buffers must be extracted AFTER glyphon has rendered them but BEFORE they are dropped. Check whether pending_buffers is cleared in prepare(), render(), or manually.
  </action>
  <verify>
  `cargo check` passes. Run `cargo run -- --show-fps` in release mode with 2+ tabs open. Observe glyph cache hit rate climbing above 0% in the title bar. Run with `--no-glyph-cache` and confirm hit rate stays at 0%.
  </verify>
  <done>
  Buffers are returned to the LRU cache after each frame's render. --show-fps displays glyph cache hit rate. --no-glyph-cache disables the cache. Scrolling through unchanged lines shows high cache hit rate.
  </done>
</task>

</tasks>

<verification>
- `cargo check` compiles cleanly
- `cargo test` passes all existing tests
- `cargo run --release -- --show-fps` shows glyph hit rate > 0% after a few frames
- `cargo run --release -- --show-fps --no-glyph-cache` shows glyph hit rate = 0%
- No visual regression in text rendering
</verification>

<success_criteria>
- Glyphon Buffer shaping is cached per (text_hash, font_size, line_height)
- Cache hit rate visible in --show-fps
- --no-glyph-cache flag disables cache for comparison
- No visual regression, no crashes
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-rendering-performance-optimization/13.1-01-SUMMARY.md`
</output>
