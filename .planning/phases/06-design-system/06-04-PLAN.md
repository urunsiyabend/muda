---
phase: 06-design-system
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - ora/src/elements/button.rs
  - ora/examples/element_library_demo.rs
autonomous: false

must_haves:
  truths:
    - "Button element uses cx.theme().color() instead of hardcoded Color::rgb() values"
    - "Button respects current theme (dark or light mode)"
    - "Demo shows theme switching at runtime via keyboard shortcut"
    - "UI visually updates when theme changes"
  artifacts:
    - path: "ora/src/elements/button.rs"
      provides: "Theme-aware button styling"
      contains: "cx.theme().color"
    - path: "ora/examples/element_library_demo.rs"
      provides: "Theme switching demo"
      contains: "ThemeChanged|set_theme"
  key_links:
    - from: "ora/src/elements/button.rs"
      to: "ora/src/theme/mod.rs"
      via: "ColorToken usage"
      pattern: "ColorToken::|theme\\(\\)\\.color"
---

<objective>
Migrate Button element from hardcoded colors to theme tokens, and update the element library demo to showcase runtime theme switching.

Purpose: Prove the design system works end-to-end: elements use tokens, theme switching triggers UI refresh, and users can see the visual difference between dark and light modes.

Output: Theme-aware Button element and demo with theme toggle functionality.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-design-system/06-CONTEXT.md
@.planning/phases/06-design-system/06-03-SUMMARY.md

@ora/src/elements/button.rs
@ora/examples/element_library_demo.rs
@ora/src/theme/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Button element to use theme tokens</name>
  <files>ora/src/elements/button.rs</files>
  <action>
Update ora/src/elements/button.rs to use theme tokens instead of hardcoded colors.

1. Add theme imports:
```rust
use crate::theme::{ColorToken, Theme};
```

2. Refactor ButtonVariant::style() to accept &Theme parameter:
```rust
impl ButtonVariant {
    /// Get the style configuration for this variant in the given state
    pub fn style(&self, state: ButtonState, theme: &Theme) -> ButtonStyle {
        match (self, state) {
            // Primary - uses accent colors from theme
            (Self::Primary, ButtonState::Enabled) => ButtonStyle {
                bg: theme.color(ColorToken::Accent),
                text: theme.color(ColorToken::BgPrimary),  // Contrast with accent
                border_color: None,
                border_width: 0.0,
            },
            (Self::Primary, ButtonState::Hover) => ButtonStyle {
                bg: theme.color(ColorToken::AccentHover),
                text: theme.color(ColorToken::BgPrimary),
                border_color: None,
                border_width: 0.0,
            },
            (Self::Primary, ButtonState::Active) => ButtonStyle {
                bg: theme.color(ColorToken::AccentActive),
                text: theme.color(ColorToken::BgPrimary),
                border_color: None,
                border_width: 0.0,
            },
            (Self::Primary, ButtonState::Disabled) => {
                let mut bg = theme.color(ColorToken::Accent);
                bg.a = 0.5;
                let mut text = theme.color(ColorToken::BgPrimary);
                text.a = 0.6;
                ButtonStyle { bg, text, border_color: None, border_width: 0.0 }
            },

            // Secondary - uses bg/fg colors from theme
            (Self::Secondary, ButtonState::Enabled) => ButtonStyle {
                bg: theme.color(ColorToken::BgSecondary),
                text: theme.color(ColorToken::FgPrimary),
                border_color: None,
                border_width: 0.0,
            },
            (Self::Secondary, ButtonState::Hover) => ButtonStyle {
                bg: theme.color(ColorToken::BgElevated),
                text: theme.color(ColorToken::FgPrimary),
                border_color: None,
                border_width: 0.0,
            },
            (Self::Secondary, ButtonState::Active) => ButtonStyle {
                bg: theme.color(ColorToken::Border),
                text: theme.color(ColorToken::FgPrimary),
                border_color: None,
                border_width: 0.0,
            },
            (Self::Secondary, ButtonState::Disabled) => {
                let mut bg = theme.color(ColorToken::BgSecondary);
                bg.a = 0.5;
                let mut text = theme.color(ColorToken::FgPrimary);
                text.a = 0.4;
                ButtonStyle { bg, text, border_color: None, border_width: 0.0 }
            },

            // Ghost - transparent with hover
            (Self::Ghost, ButtonState::Enabled) => ButtonStyle {
                bg: Color::transparent(),
                text: theme.color(ColorToken::FgPrimary),
                border_color: None,
                border_width: 0.0,
            },
            (Self::Ghost, ButtonState::Hover) => {
                let mut bg = theme.color(ColorToken::FgPrimary);
                bg.a = 0.1;
                ButtonStyle {
                    bg,
                    text: theme.color(ColorToken::FgPrimary),
                    border_color: None,
                    border_width: 0.0,
                }
            },
            (Self::Ghost, ButtonState::Active) => {
                let mut bg = theme.color(ColorToken::FgPrimary);
                bg.a = 0.2;
                ButtonStyle {
                    bg,
                    text: theme.color(ColorToken::FgPrimary),
                    border_color: None,
                    border_width: 0.0,
                }
            },
            (Self::Ghost, ButtonState::Disabled) => {
                let mut text = theme.color(ColorToken::FgPrimary);
                text.a = 0.4;
                ButtonStyle {
                    bg: Color::transparent(),
                    text,
                    border_color: None,
                    border_width: 0.0,
                }
            },

            // Destructive - uses error color from theme
            (Self::Destructive, ButtonState::Enabled) => ButtonStyle {
                bg: theme.color(ColorToken::Error),
                text: theme.color(ColorToken::BgPrimary),
                border_color: None,
                border_width: 0.0,
            },
            (Self::Destructive, ButtonState::Hover) => {
                // Slightly darker error for hover
                let mut bg = theme.color(ColorToken::Error);
                bg.r *= 0.9;
                bg.g *= 0.9;
                bg.b *= 0.9;
                ButtonStyle {
                    bg,
                    text: theme.color(ColorToken::BgPrimary),
                    border_color: None,
                    border_width: 0.0,
                }
            },
            (Self::Destructive, ButtonState::Active) => {
                let mut bg = theme.color(ColorToken::Error);
                bg.r *= 0.8;
                bg.g *= 0.8;
                bg.b *= 0.8;
                ButtonStyle {
                    bg,
                    text: theme.color(ColorToken::BgPrimary),
                    border_color: None,
                    border_width: 0.0,
                }
            },
            (Self::Destructive, ButtonState::Disabled) => {
                let mut bg = theme.color(ColorToken::Error);
                bg.a = 0.5;
                let mut text = theme.color(ColorToken::BgPrimary);
                text.a = 0.6;
                ButtonStyle { bg, text, border_color: None, border_width: 0.0 }
            },
        }
    }
}
```

3. Update Button::paint() to pass theme to variant.style():
```rust
fn paint(&mut self, state: &mut Self::RequestLayoutState, cx: &mut PaintContext) {
    let bounds = cx.bounds(state.layout_id);

    // Determine button state from interaction state
    let button_state = if self.disabled {
        ButtonState::Disabled
    } else if let Some(hitbox_id) = state.hitbox_id {
        if cx.is_active(hitbox_id) {
            ButtonState::Active
        } else if cx.is_hovered(hitbox_id) {
            ButtonState::Hover
        } else {
            ButtonState::Enabled
        }
    } else {
        ButtonState::Enabled
    };

    // Get variant-specific style WITH THEME
    let button_style = self.variant.style(button_state, cx.theme());

    // ... rest of paint method unchanged
}
```

4. Also update request_layout to use theme for text color (note: LayoutContext doesn't have theme, so we may need to store variant and compute color at paint time, or pass a default). For now, keep the existing pattern but note that text color is computed at paint time when text element paints.

Actually, looking at the current code, the text color is set during request_layout. Since LayoutContext doesn't have theme access (per RESEARCH.md: access theme during paint, not layout), we should:
- Keep the text element creation in request_layout
- Update text color during paint by modifying the stored text_element

But TextElement doesn't have a mutable color setter. Simpler approach: store the variant and let Button handle text painting directly with the correct color. However, that's a larger refactor.

For this plan, accept the limitation: text color will be theme-default at layout time. A future enhancement could add theme to LayoutContext or defer text styling to paint. Document this as a known limitation.

Alternative simpler fix: Since TextElement.paint() just renders what was measured, and text color doesn't affect measurement, we could add a set_color() method to TextElement. But that changes the Element API.

Pragmatic solution for this plan: In paint(), after getting button_style, update the text_element's color field before calling its paint(). This requires making TextElement.color public or adding a setter.

Add to TextElement (ora/src/elements/text.rs):
```rust
/// Set the text color (for use during paint phase)
pub fn set_color(&mut self, color: Color) {
    self.color = color;
}
```

Then in Button::paint():
```rust
// Update text color based on theme before painting
if let Some(text_element) = &mut self.text_element {
    text_element.set_color(button_style.text);
    text_element.paint(&mut state.text_state, cx);
}
```
  </action>
  <verify>`cargo check -p ora` compiles without errors</verify>
  <done>Button uses cx.theme().color(ColorToken::*) for all colors, text color updated at paint time to match button state</done>
</task>

<task type="auto">
  <name>Task 2: Add theme toggle to element library demo</name>
  <files>ora/examples/element_library_demo.rs</files>
  <action>
Update ora/examples/element_library_demo.rs to demonstrate runtime theme switching.

1. Add theme toggle action and keybinding:
```rust
// Define action for theme toggle
#[derive(Clone, Debug)]
struct ToggleTheme;
impl Action for ToggleTheme {
    fn boxed_clone(&self) -> Box<dyn Action> {
        Box::new(self.clone())
    }
}
```

2. In the view setup, register action handler and keybinding:
```rust
// Bind 'T' key to toggle theme
cx.bind_key(
    Keystroke {
        key: Key::Character("t".into()),
        modifiers: Modifiers::default(),
    },
    Box::new(ToggleTheme),
);

cx.on_action::<ToggleTheme>(move |_action| {
    // Theme toggle happens via global event subscription
});
```

3. Subscribe to ToggleTheme globally or handle in window context. Since actions are dispatched through AppContext, add a mechanism to toggle:

Actually, the cleanest approach is to track current theme mode in view state and toggle on action:

In the demo view struct, add:
```rust
is_dark: bool,
```

Initialize to true (dark mode default).

In action handler or via a model update, toggle:
```rust
// When ToggleTheme action fires
self.is_dark = !self.is_dark;
let new_theme = if self.is_dark { Theme::dark() } else { Theme::light() };
// Need access to cx to call set_theme()
```

The challenge: action handlers don't have direct cx access in the current design.

Simpler approach for demo: Add a "Toggle Theme" button that calls set_theme when clicked. Buttons have on_click handlers.

In render():
```rust
// Add a toggle theme button
button("Toggle Theme (T)")
    .secondary()
    .on_click({
        // We need a way to communicate back to window context
        // For demo purposes, we can use a static AtomicBool or similar
    })
```

Actually, the cleanest demo approach given current architecture:
- Use the existing element_library_demo.rs structure
- Add a Model<ThemeState> to track current mode
- Subscribe to model changes to call set_theme
- Button on_click updates the model

Let me trace the current demo structure... The demo likely has a root view. The view can hold a Model<bool> for dark mode, and in window setup, observe that model to call set_theme.

Concrete implementation:

1. In demo, create a Model to track theme:
```rust
struct ThemeToggle {
    is_dark: bool,
}
```

2. In window setup:
```rust
let theme_model = cx.new_model(ThemeToggle { is_dark: true });

// Clone for observer
let theme_model_clone = theme_model.clone();
cx.observe(&theme_model, move |cx| {
    let is_dark = cx.read_model(&theme_model_clone).is_dark;
    if is_dark {
        cx.set_theme(Theme::dark());
    } else {
        cx.set_theme(Theme::light());
    }
}).detach();
```

Wait, observe callback gets &mut AppContext but WindowContext has set_theme. Need to add set_theme to AppContext directly for this pattern.

AppContext already has set_theme (added in Task 1). So the observer pattern works:
```rust
cx.observe(&theme_model, move |app_cx: &mut AppContext| {
    let is_dark = app_cx.read_model(&theme_model_clone).is_dark;
    let theme = if is_dark { Theme::dark() } else { Theme::light() };
    app_cx.set_theme(theme);
}).detach();
```

3. In view, store theme_model and use in button:
```rust
button("Toggle Theme")
    .secondary()
    .on_click({
        let theme_model = self.theme_model.clone();
        move || {
            // Problem: on_click closure doesn't have cx access
        }
    })
```

The on_click pattern doesn't have context access. This is a limitation of the current Button design.

For the demo, simplest approach: add a label showing "Press T to toggle theme" and handle via keyboard action. Or, rework on_click to receive context.

Given time constraints, let's use a different approach:
- Add global ThemeChanged subscription that logs theme change
- Modify the demo to toggle theme via keyboard ('T' key)
- Handle 'T' key in window event handling code

In element_library_demo.rs, in the event handling section (if there is one), or add keyboard handling:

Looking at existing demos, they typically handle events in run loop. The simplest addition:
- Check for 'T' keypress in the main loop
- Toggle theme directly via app context

This is a demo, so direct handling is acceptable:

```rust
// In event handling
WindowEvent::KeyboardInput { event, .. } => {
    if event.state == ElementState::Pressed {
        if let Key::Character(c) = &event.logical_key {
            if c == "t" || c == "T" {
                // Toggle theme
                let current_mode = app_context.theme().mode();
                let new_theme = match current_mode {
                    ThemeMode::Dark => Theme::light(),
                    ThemeMode::Light => Theme::dark(),
                };
                app_context.set_theme(new_theme);
                window.request_redraw();
            }
        }
    }
}
```

4. Update demo UI to show current theme mode in title or label:
```rust
text(format!("Theme: {:?}", cx.theme().mode()))
```

5. Update the demo background color to use theme:
Instead of hardcoded clear color, use theme.color(ColorToken::BgPrimary) for the window clear color.
  </action>
  <verify>`cargo run --example element_library_demo` runs. Press 'T' to toggle theme. UI colors change.</verify>
  <done>Demo shows theme toggle via 'T' key, button colors update based on theme, background updates based on theme</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Theme-aware Button element and demo with runtime theme switching:
1. Button element uses cx.theme().color(ColorToken::*) for all styling
2. Demo has 'T' key to toggle between dark and light themes
3. UI visually updates when theme changes
  </what-built>
  <how-to-verify>
1. Run `cargo run --example element_library_demo`
2. Observe initial dark theme (dark background, light text)
3. Press 'T' key
4. Verify background switches to light (white/gray) and buttons adapt
5. Press 'T' again to return to dark theme
6. Hover over buttons to verify hover states work in both themes
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues</resume-signal>
</task>

</tasks>

<verification>
1. `cargo check -p ora` compiles without errors
2. `cargo test -p ora` passes all existing tests
3. Button.rs contains `cx.theme().color(ColorToken::` patterns
4. Demo runs and responds to 'T' key for theme toggle
5. Visual: buttons show different colors in dark vs light theme
</verification>

<success_criteria>
- Button element retrieves all colors from theme tokens
- No hardcoded Color::rgb() values remain in button.rs (except for transparent)
- Demo demonstrates runtime theme switching
- UI visually updates when theme changes (background, button colors)
- Theme state persists across renders until explicitly changed
</success_criteria>

<output>
After completion, create `.planning/phases/06-design-system/06-04-SUMMARY.md`
</output>
