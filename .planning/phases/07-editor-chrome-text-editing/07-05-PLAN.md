---
phase: 07-editor-chrome-text-editing
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - ora/examples/editor_chrome_demo.rs
  - ora/src/views/mod.rs
autonomous: false

must_haves:
  truths:
    - "Demo shows complete editor chrome layout with all Phase 7 views"
    - "All components render using theme tokens (dark/light modes)"
    - "TabBar, StatusBar, Sidebar, Gutter, TextArea, Dialog visible and functional"
  artifacts:
    - path: "ora/examples/editor_chrome_demo.rs"
      provides: "Integration demo for Phase 7 components"
      min_lines: 200
  key_links:
    - from: "ora/examples/editor_chrome_demo.rs"
      to: "ora::views::*"
      via: "imports and composes all Phase 7 views"
      pattern: "TabBarView|StatusBarView|SidebarView|GutterView|DialogView|TextAreaView"
---

<objective>
Create integration demo and verify all Phase 7 components work together.

Purpose: Demonstrates complete editor chrome layout with all migrated views, validates theme token usage, and provides visual verification checkpoint.

Output: editor_chrome_demo.rs showing full editor layout with tabs, sidebar, gutter, text area, status bar, and dialog overlay.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-editor-chrome-text-editing/07-CONTEXT.md
@ora/examples/element_library_demo.rs
@ora/src/views/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create editor chrome demo layout</name>
  <files>ora/examples/editor_chrome_demo.rs</files>
  <action>
    1. Create editor_chrome_demo.rs similar to element_library_demo.rs structure:
       - App::new("Editor Chrome Demo", 1200, 800)
       - Root view: EditorChromeDemo implementing View

    2. EditorChromeDemo struct holds:
       - Model<MockEditorState> with tab_bar, status, gutter, visible_lines, dialog, sidebar

    3. MockEditorState for demo data:
       ```rust
       struct MockEditorState {
           tab_bar: TabBarPresentation,
           status: StatusPresentation,
           gutter: GutterModel,
           visible_lines: Vec<LinePresentation>,
           caret: CaretPresentation,
           dialog: DialogPresentation,
           sidebar: SidebarPresentation,
       }
       ```

    4. Create sample presentation data:
       - TabBar: 3 tabs ("main.rs", "*untitled.txt", "lib.rs"), second is dirty
       - StatusBar: Ln 42, Col 15, Rust, UTF-8, 156 lines
       - Gutter: visible, 156 total lines (3 digits needed)
       - VisibleLines: 20 sample lines with syntax highlighting
       - Sidebar: visible, expanded, "my_project" directory
       - Dialog: Initially DialogPresentation::None

    5. Build main layout using Div hierarchy:
       ```
       Column (full window)
       ├── TabBarView (28px height)
       ├── Row (flex: 1)
       │   ├── SidebarView (220px or 48px collapsed)
       │   ├── Column (editor area)
       │   │   ├── Row (gutter + text)
       │   │   │   ├── GutterView
       │   │   │   └── TextAreaView
       │   ├── (future: right panel placeholder)
       ├── StatusBarView (24px height)
       └── DialogView (overlay, conditionally rendered)
       ```

    6. Add keyboard handlers:
       - 'T' key: toggle theme (dark/light)
       - 'D' key: toggle dialog visibility
       - 'S' key: toggle sidebar collapsed/expanded
       - 'Tab': cycle through tabs

    7. Ensure all views use theme tokens (no hardcoded colors)
  </action>
  <verify>
    cargo build --example editor_chrome_demo -p ora
    cargo run --example editor_chrome_demo -p ora
    Visual inspection: all components render correctly
  </verify>
  <done>Demo compiles and shows basic layout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete editor chrome demo with all Phase 7 views integrated</what-built>
  <how-to-verify>
    Run: cargo run --example editor_chrome_demo -p ora

    Verify each component:
    1. **Tab Bar** (top):
       - Shows 3 tabs with titles
       - One tab has dirty indicator ("●")
       - Active tab has distinct background

    2. **Sidebar** (left):
       - Shows "Explorer" header with toggle button
       - Press 'S' to collapse → shows narrow icon rail (48px)
       - Press 'S' again to expand → shows full sidebar (220px)

    3. **Gutter** (left of text):
       - Shows line numbers right-aligned
       - Current line number is brighter

    4. **Text Area** (main area):
       - Shows syntax-highlighted text (if sample data includes highlighting)
       - Current line has background highlight
       - Caret visible at cursor position

    5. **Status Bar** (bottom):
       - Shows "Ln 42, Col 15" on left
       - Shows "Rust │ UTF-8 │ 156 lines" on right

    6. **Dialog** (overlay):
       - Press 'D' to show dialog
       - Semi-transparent backdrop visible
       - Centered dialog with title, message, buttons
       - Buttons right-aligned: Save, Don't Save, Cancel

    7. **Theme Toggle**:
       - Press 'T' to toggle dark/light theme
       - All components update colors appropriately

    Report any visual issues or missing components.
  </how-to-verify>
  <resume-signal>Type "verified" if all components render correctly, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. cargo build --example editor_chrome_demo -p ora — compiles
2. cargo run shows all Phase 7 views in proper layout
3. Theme toggle ('T') switches all components between dark/light
4. Dialog overlay ('D') shows/hides correctly
5. Sidebar collapse ('S') works
6. All text uses semantic colors from theme
</verification>

<success_criteria>
- Demo shows complete editor layout with all Phase 7 components
- Layout matches expected structure (TabBar top, Sidebar left, Gutter+TextArea center, StatusBar bottom)
- Dialog renders as overlay when activated
- Theme switching updates all components
- No hardcoded colors visible in demo code
</success_criteria>

<output>
After completion, create `.planning/phases/07-editor-chrome-text-editing/07-05-SUMMARY.md`
</output>
