---
phase: 07-editor-chrome-text-editing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ora/src/views/dialog.rs
  - ora/src/views/mod.rs
  - ora/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "DialogView renders modal with semi-transparent backdrop"
    - "Dialog has right-aligned buttons: Save/Don't Save/Cancel"
    - "Escape key and backdrop click do NOT dismiss dialog per CONTEXT"
  artifacts:
    - path: "ora/src/views/dialog.rs"
      provides: "DialogView implementation"
      min_lines: 120
  key_links:
    - from: "ora/src/views/dialog.rs"
      to: "core_editor::view_model::DialogPresentation"
      via: "render consumes dialog state"
      pattern: "DialogPresentation|UnsavedChangesConfirmation"
    - from: "ora/src/views/dialog.rs"
      to: "ora::stack"
      via: "uses Stack for z-layered overlay"
      pattern: "stack\\(\\)|Stack"
---

<objective>
Implement DialogView as ora View for modal overlay dialogs.

Purpose: Migrates the unsaved changes confirmation dialog from wgpu_client to ora, using Stack for z-layering with backdrop and centered dialog box.

Output: DialogView showing modal with semi-transparent backdrop, title, message, and action buttons following CONTEXT.md decisions.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-editor-chrome-text-editing/07-CONTEXT.md
@.planning/phases/07-editor-chrome-text-editing/07-RESEARCH.md
@ora/src/elements/stack.rs
@ora/src/elements/button.rs
@core_editor/src/view_model/mod.rs
@wgpu_client/src/components/dialog.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DialogView</name>
  <files>ora/src/views/dialog.rs, ora/src/views/mod.rs, ora/src/lib.rs</files>
  <action>
    1. Create dialog.rs implementing DialogView struct:
       - Store FocusHandle for each button: save_focus, dont_save_focus, cancel_focus
       - Constants from wgpu_client:
         - DIALOG_WIDTH: 420px
         - DIALOG_PADDING: 24px
         - BUTTON_HEIGHT: 36px
         - BUTTON_MIN_WIDTH: 110px
         - BUTTON_SPACING: 12px

    2. Implement View::render() consuming DialogPresentation:
       - If DialogPresentation::None: return empty Div (or skip rendering entirely)
       - If UnsavedChangesConfirmation: render full modal

    3. Use Stack for z-layering (RESEARCH.md Pattern 4):
       ```rust
       stack()
           // Layer 0: Backdrop
           .child(self.render_backdrop(cx))
           // Layer 1: Dialog box
           .child(self.render_dialog_box(dialog, cx))
           .into()
       ```

    4. render_backdrop():
       ```rust
       Div::new()
           .absolute()
           .w(pct(100.0))
           .h(pct(100.0))
           .bg(Color::from_rgba(0.0, 0.0, 0.0, 0.6))  // 60% opacity
           // NO on_click handler - CONTEXT: must use buttons
       ```

    5. render_dialog_box():
       - Centered on screen (absolute positioning or flex centering)
       - Width: 420px fixed
       - Background: theme.color(ColorToken::BgPrimary)
       - Border radius: 8px
       - Optional shadow (if Div supports box_shadow)
       - Contains: title bar, content, button row

    6. render_title_bar():
       - "Unsaved Changes" text, bold
       - Warning accent strip (4px left border in warning color)
       - Height: 36px
       - Background: slightly darker than dialog body

    7. render_content():
       - Message: "Do you want to save changes before {action}?"
       - List of unsaved documents if any
       - Padding: 24px

    8. render_button_row():
       - CONTEXT decision: right-aligned buttons
       - Use JustifyContent::End
       - Three buttons with 12px gap:
         - "Save (Y)" - ButtonVariant::Primary
         - "Don't Save (N)" - ButtonVariant::Secondary
         - "Cancel (Esc)" - ButtonVariant::Ghost
       - NOTE: Button handlers can't access context (known blocker from STATE.md)
         For now, buttons render but handlers emit events to parent view

    9. Export DialogView from views/mod.rs and ora/src/lib.rs

    CONTEXT locked decisions:
    - Escape key does NOT dismiss (must use Cancel button)
    - Backdrop click does NOT dismiss (must use buttons)
  </action>
  <verify>
    cargo check -p ora
    cargo test -p ora
    Verify: DialogView compiles, uses Stack for layering
  </verify>
  <done>DialogView renders modal with backdrop and right-aligned buttons</done>
</task>

<task type="auto">
  <name>Task 2: Add ColorToken for dialog-specific colors</name>
  <files>ora/src/theme/color.rs</files>
  <action>
    Review if additional ColorToken variants are needed for dialogs:
    - Warning accent color (yellow/orange for unsaved changes indicator)
    - Backdrop overlay color (if not using raw RGBA)

    Options:
    A) Add ColorToken::Warning for the accent strip
    B) Add ColorToken::Backdrop for overlay
    C) Use existing tokens + raw Color for dialog-specific colors

    Likely approach: Add ColorToken::Warning (useful for other UI elements too).
    Keep backdrop as raw Color::from_rgba since it's always semi-transparent black.

    If adding Warning:
    ```rust
    // In ColorToken enum
    Warning,

    // In Theme::color() match
    ColorToken::Warning => self.palette.yellow_500,  // Or similar warm color
    ```

    Update palette if yellow/warning colors don't exist.
  </action>
  <verify>
    cargo check -p ora
    cargo test -p ora
  </verify>
  <done>Dialog uses appropriate theme tokens including Warning accent</done>
</task>

</tasks>

<verification>
1. cargo check -p ora — compiles without errors
2. cargo test -p ora — existing tests still pass
3. DialogView uses Stack for backdrop + dialog layering
4. Backdrop is semi-transparent (60% opacity black)
5. Buttons are right-aligned per CONTEXT decision
6. No on_click handlers on backdrop (modal is NOT dismissible by backdrop click)
</verification>

<success_criteria>
- DialogView renders modal overlay using Stack element
- Backdrop covers full screen with semi-transparency
- Dialog box centered with title, content, button row
- Buttons right-aligned: Save, Don't Save, Cancel
- No escape-dismissal or backdrop-dismissal per CONTEXT
</success_criteria>

<output>
After completion, create `.planning/phases/07-editor-chrome-text-editing/07-03-SUMMARY.md`
</output>
