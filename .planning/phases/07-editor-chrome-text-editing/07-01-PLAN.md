---
phase: 07-editor-chrome-text-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ora/src/views/mod.rs
  - ora/src/views/tab_bar.rs
  - ora/src/views/status_bar.rs
  - ora/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "TabBarView renders tabs with titles, dirty indicators, and active state styling"
    - "StatusBarView displays cursor position, language, encoding in single row"
    - "Both views use theme tokens for all colors (no hardcoded RGB)"
  artifacts:
    - path: "ora/src/views/mod.rs"
      provides: "Views module declaration"
      exports: ["TabBarView", "StatusBarView"]
    - path: "ora/src/views/tab_bar.rs"
      provides: "TabBarView implementation"
      min_lines: 80
    - path: "ora/src/views/status_bar.rs"
      provides: "StatusBarView implementation"
      min_lines: 60
  key_links:
    - from: "ora/src/views/tab_bar.rs"
      to: "core_editor::view_model::TabBarPresentation"
      via: "render consumes presentation data"
      pattern: "TabBarPresentation|TabPresentation"
    - from: "ora/src/views/status_bar.rs"
      to: "core_editor::view_model::StatusPresentation"
      via: "render consumes presentation data"
      pattern: "StatusPresentation"
---

<objective>
Create views module and implement TabBarView and StatusBarView as ora Views.

Purpose: Establishes the views/ module pattern for Phase 7 components and migrates two foundational horizontal chrome components from wgpu_client to ora.

Output: TabBarView showing open tabs with styling, StatusBarView showing editor status - both consuming core_editor presentation types and using ora elements (Div, TextElement, Button).
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-editor-chrome-text-editing/07-RESEARCH.md
@ora/src/elements/div.rs
@ora/src/elements/button.rs
@ora/src/view/trait_def.rs
@core_editor/src/view_model/mod.rs
@wgpu_client/src/components/tab_bar.rs
@wgpu_client/src/components/status_bar.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create views module with TabBarView</name>
  <files>ora/src/views/mod.rs, ora/src/views/tab_bar.rs, ora/src/lib.rs</files>
  <action>
    1. Create ora/src/views/ directory and mod.rs
    2. Create tab_bar.rs implementing TabBarView struct with View trait:
       - Store Model<T> or direct reference to TabBarPresentation (decide based on usage pattern)
       - Implement View::render() returning Div.flex_row() with child tabs
       - Each tab is Div with:
         - Dirty indicator ("●" prefix if is_dirty)
         - Title text using TextElement
         - Active state: theme.color(ColorToken::BgPrimary) background
         - Inactive state: theme.color(ColorToken::BgSecondary) background
         - Hover state: use Div.hover_bg() for subtle highlight
       - Tab bar height: 28px (from wgpu_client const)
       - Use theme tokens: BgSecondary for bar background, FgPrimary for text
       - Bottom border: 1px with BorderDefault color
    3. Export TabBarView from mod.rs
    4. Add "pub mod views;" to ora/src/lib.rs
    5. Re-export TabBarView from ora/src/lib.rs

    Pattern from RESEARCH.md:
    ```rust
    impl View for TabBarView {
        fn render(&self, cx: &mut ViewContext) -> AnyElement {
            let theme = cx.theme();
            Div::new()
                .flex_row()
                .h(px(28.0))
                .bg(theme.color(ColorToken::BgSecondary))
                .children(...)
                .into()
        }
    }
    ```

    Note: Close buttons deferred to Phase 8 (needs proper button handlers with context).
    Tab overflow dropdown deferred to Phase 8 (needs dropdown/menu component).
  </action>
  <verify>
    cargo check -p ora
    Verify: TabBarView compiles, uses View trait, renders element tree
  </verify>
  <done>TabBarView renders tabs with titles, dirty indicators, and theme-aware styling</done>
</task>

<task type="auto">
  <name>Task 2: Create StatusBarView</name>
  <files>ora/src/views/status_bar.rs, ora/src/views/mod.rs, ora/src/lib.rs</files>
  <action>
    1. Create status_bar.rs implementing StatusBarView struct with View trait:
       - StatusBarView consumes StatusPresentation from core_editor
       - Implement View::render() returning Div.flex_row() with:
         - Left section: cursor position "Ln {line}, Col {column}"
         - Right section: language, encoding, total_lines (separated by " │ ")
         - Use JustifyContent::SpaceBetween for left/right distribution
       - Height: 24px (from wgpu_client const)
       - Background: theme.color(ColorToken::BgSecondary)
       - Text: theme.color(ColorToken::FgSecondary) with TextSize::Small (12px)
       - Padding: horizontal 8px
    2. Add StatusBarView export to views/mod.rs
    3. Re-export StatusBarView from ora/src/lib.rs

    Pattern:
    ```rust
    impl View for StatusBarView {
        fn render(&self, cx: &mut ViewContext) -> AnyElement {
            let theme = cx.theme();
            Div::new()
                .flex_row()
                .w(pct(100.0))
                .h(px(24.0))
                .bg(theme.color(ColorToken::BgSecondary))
                .justify(JustifyContent::SpaceBetween)
                .p(8.0)
                .child(/* left */)
                .child(/* right */)
                .into()
        }
    }
    ```

    Handle optional message: if status.message is Some, show it instead of cursor/language info.
  </action>
  <verify>
    cargo check -p ora
    cargo test -p ora
    Verify: StatusBarView compiles, shows cursor position and language info
  </verify>
  <done>StatusBarView displays cursor position and language info with theme-aware styling</done>
</task>

</tasks>

<verification>
1. cargo check -p ora — compiles without errors
2. cargo test -p ora — existing tests still pass
3. ora/src/views/ directory exists with mod.rs, tab_bar.rs, status_bar.rs
4. Both views use View trait correctly
5. No direct glyphon usage (use TextElement only)
6. No hardcoded colors (use ColorToken semantic tokens)
</verification>

<success_criteria>
- TabBarView and StatusBarView compile and implement View trait
- Both use ora elements (Div, TextElement) not raw glyphon
- Both use theme tokens (ColorToken::*) not hardcoded RGB
- Pattern established for remaining Phase 7 views
</success_criteria>

<output>
After completion, create `.planning/phases/07-editor-chrome-text-editing/07-01-SUMMARY.md`
</output>
