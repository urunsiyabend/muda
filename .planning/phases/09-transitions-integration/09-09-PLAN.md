---
phase: 09-transitions-integration
plan: 09
type: execute
wave: 6
depends_on: ["09-08"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Elements animate opacity, color, background-color on state changes with easing"
    - "Hover/active transitions smoothly fade between default, hovered, and pressed states"
    - "wgpu_client is reduced to thin app shell"
    - "ora views consume RenderModel from adapter, not core_editor directly"
    - "Keyboard events translate to EditorCommand and dispatch to core_editor"
    - "All hardcoded values eliminated from wgpu_client"
    - "All duplicated styling logic removed from wgpu_client"
  artifacts: []
  key_links: []
---

<objective>
Final verification checkpoint: visually and functionally verify all Phase 9 requirements are met.

Purpose: Confirm transitions work visually, integration is correct, and the editor functions properly through the adapter boundary.

Output: Verified Phase 9 complete.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 9 requirements</name>
  <files></files>
  <action>
  Run comprehensive automated checks:

  1. **Build verification:**
     - `cargo check --workspace` — full workspace compiles
     - `cargo test --workspace` — all tests pass
     - `cargo clippy -p ora` — no warnings

  2. **Transition system (TRANS-01, TRANS-02, TRANS-03):**
     - Verify `ora::animation::Tween`, `Easing`, `CubicBezier` exist and are public
     - Verify `ora::animation::TransitionSpec`, `TransitionState`, `TransitionRegistry` exist
     - Verify `Div` has `transition_bg`, `transition_opacity`, `transition_color`, `transition_id` methods
     - Run animation tests: `cargo test -p ora --lib animation`

  3. **Hover/active transitions (TRANS-04):**
     - Verify Button, Tab elements use `transition_bg` (grep for transition_bg in views/)
     - `grep -r "transition_bg\|transition_id" ora/src/views/ ora/src/elements/button.rs`

  4. **Adapter boundary (INT-02, INT-03):**
     - `grep -r "use core_editor" ora/src/` returns zero results (or only dev-dependencies)
     - Verify `EditorDataSource` trait exists in ora::editor_adapter
     - Verify `CoreEditorAdapter` exists in wgpu_client/src/adapter.rs

  5. **Thin shell (INT-01):**
     - `wc -l wgpu_client/src/main.rs` is under 40 lines
     - Count files in wgpu_client/src/: should be main.rs + adapter.rs + minimal support

  6. **No hardcoded values (INT-04):**
     - `grep -rn "[0-9]\{2,\}\.[0-9]" wgpu_client/src/ --include="*.rs"` — no UI measurement literals
     - Exclude legitimate uses (version numbers, log messages)

  7. **No duplicated styling (INT-05):**
     - No design_system directory in wgpu_client
     - No theme directory in wgpu_client
     - No color token definitions in wgpu_client

  Report results for each check.
  </action>
  <verify>All 7 verification areas pass</verify>
  <done>Automated verification confirms all Phase 9 requirements are met</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 9: CSS-like transitions and wgpu_client-to-ora migration. Transitions animate hover/active states smoothly. wgpu_client is a thin shell. All UI flows through ora's adapter boundary.</what-built>
  <how-to-verify>
  1. Run `cargo run -p wgpu_client` to launch the editor
  2. Verify editor opens and displays content correctly (text, syntax highlighting, gutter, tabs, status bar)
  3. Hover over a tab — verify smooth color transition (not instant snap)
  4. Hover over buttons in sidebar or dialog — verify smooth transitions
  5. Click elements — verify active state transitions smoothly
  6. Type text — verify keyboard input works through the adapter
  7. Open command palette — verify it still functions
  8. Toggle theme (if applicable) — verify instant swap (NOT animated per CONTEXT)
  9. General: editor feels responsive and polished, no visual regressions from Phase 8
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
All Phase 9 success criteria verified:
1. Elements animate opacity, color, background-color on state changes with easing functions
2. Hover/active transitions smoothly fade between default, hovered, and pressed states
3. wgpu_client is reduced to thin app shell
4. ora views consume RenderModel from adapter
5. Keyboard events translate to EditorCommand
6. All hardcoded values eliminated
7. All duplicated styling logic removed
</verification>

<success_criteria>
- Full editor works through ora + adapter
- Smooth transitions visible on hover/active
- No visual regressions
- User approves at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/09-transitions-integration/09-09-SUMMARY.md`
</output>
