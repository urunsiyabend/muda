---
phase: 09-transitions-integration
plan: 07
type: execute
wave: 4
depends_on: ["09-06"]
files_modified:
  - ora/src/views/tab_bar.rs
  - ora/src/views/sidebar.rs
  - ora/src/views/command_palette.rs
  - ora/src/views/dialog.rs
  - ora/src/views/status_bar.rs
  - ora/src/elements/button.rs
  - ora/src/animation/transition.rs
autonomous: true

must_haves:
  truths:
    - "Hover states on interactive elements (buttons, tabs, list items) smoothly fade between colors"
    - "Active/pressed states transition smoothly, not instant"
    - "Transition durations are 150-250ms range per CONTEXT decision"
  artifacts:
    - path: "ora/src/views/tab_bar.rs"
      provides: "Tabs with smooth hover/active transitions"
      contains: "transition_bg"
    - path: "ora/src/elements/button.rs"
      provides: "Button with smooth state transitions"
      contains: "transition_bg"
  key_links:
    - from: "ora/src/views/tab_bar.rs"
      to: "ora/src/animation/transition.rs"
      via: "TransitionId assigned per tab element"
      pattern: "TransitionId"
---

<objective>
Apply transitions to interactive elements throughout the UI: buttons, tabs, sidebar items, command palette items, and dialog backdrop/body.

Purpose: This makes the entire UI feel polished with smooth state transitions instead of instant color switches. TRANS-04 (hover/active transitions) is satisfied here.

Output: All interactive elements use transition_bg with 150-250ms durations. Visual polish across the editor.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-transitions-integration/09-RESEARCH.md
@.planning/phases/09-transitions-integration/09-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transitions to Button and navigation widgets</name>
  <files>ora/src/elements/button.rs, ora/src/views/tab_bar.rs, ora/src/views/sidebar.rs, ora/src/views/status_bar.rs</files>
  <action>
  For each interactive element, add transition_id and transition_bg to the Div that has hover_bg/active_bg:

  1. **Button** (`ora/src/elements/button.rs`):
     - Button creates a Div internally. Add `.transition_id(TransitionId(hash))` and `.transition_bg(150)` to the Div.
     - The TransitionId needs to be stable per button instance. Since buttons are recreated each frame, the caller must pass in a stable ID. Add a `transition_id: Option<TransitionId>` field to Button with a `.transition_id()` builder method. If set, apply it to the inner Div.
     - Duration: 150ms for bg transitions (fast, responsive feel).

  2. **TabBarView** (`ora/src/views/tab_bar.rs`):
     - Each tab's Div should get `.transition_id(TransitionId(tab.view_id))` (view_id is already stable across frames) and `.transition_bg(150)`.

  3. **SidebarView** (`ora/src/views/sidebar.rs`):
     - Sidebar toggle button and file entries that have hover states: add transition_bg(150) with TransitionId derived from list index or name hash.

  4. **StatusBarView** (`ora/src/views/status_bar.rs`):
     - If any interactive elements have hover states, add transitions. Status bar is mostly static, so this may be minimal.

  For TransitionId generation, use a simple approach:
  - For items with stable IDs (tabs: view_id), use `TransitionId(view_id)`
  - For list items (sidebar entries), use `TransitionId(base_id + index as u64)` where base_id is a constant unique to each view (e.g., 10000 for sidebar, 20000 for tabs)
  - This avoids collisions and is deterministic across frames
  </action>
  <verify>`cargo check -p ora` compiles. Existing tests pass.</verify>
  <done>Button, Tab, and Sidebar interactive elements all use transition_bg for smooth hover/active state changes</done>
</task>

<task type="auto">
  <name>Task 2: Add transitions to overlay elements (CommandPalette, Dialog)</name>
  <files>ora/src/views/command_palette.rs, ora/src/views/dialog.rs, ora/src/animation/transition.rs</files>
  <action>
  1. **CommandPalette** (`ora/src/views/command_palette.rs`):
     - The filtered result items should have hover transitions: `.transition_id(TransitionId(30000 + index))` `.transition_bg(150)`
     - The palette container backdrop could have opacity transition if desired, but keep it simple for v1

  2. **Dialog** (`ora/src/views/dialog.rs`):
     - Dialog buttons (Save, Don't Save, Cancel) should have transition_bg(150)
     - Use TransitionId(40000 + button_index) for stable IDs

  3. **Enter/exit transitions** (stretch goal — implement if straightforward):
     Add an `AnimationPhase` enum and `EnterExitTween` to transition.rs:
     ```rust
     pub enum AnimationPhase { Entering, Visible, Exiting, Hidden }
     pub struct EnterExitTween {
         pub phase: AnimationPhase,
         pub tween: Tween<f32>, // 0.0 (hidden) to 1.0 (visible)
     }
     ```
     Toast and Dialog views can use this to fade in/out. During paint, multiply the container's opacity by the tween value. If the phase is Exiting and tween completes, set to Hidden.

     If enter/exit adds too much complexity, skip it — hover/active transitions are the primary requirement (TRANS-04). Enter/exit can be added in a gap closure plan.
  </action>
  <verify>`cargo check -p ora` compiles. `cargo test -p ora` passes.</verify>
  <done>All overlay interactive elements have smooth transitions. Hover states across the UI animate smoothly.</done>
</task>

</tasks>

<verification>
- `cargo check -p ora` compiles
- `cargo test -p ora` passes
- All interactive Divs (buttons, tabs, sidebar items, palette items, dialog buttons) have transition_bg
- TransitionIds are stable across frames and don't collide between views
</verification>

<success_criteria>
- Hovering over a button triggers a smooth 150ms color transition (not instant)
- Hovering over a tab smoothly fades between default and hover colors
- Active/pressed states also transition smoothly
- No visual regression in non-interactive elements
</success_criteria>

<output>
After completion, create `.planning/phases/09-transitions-integration/09-07-SUMMARY.md`
</output>
