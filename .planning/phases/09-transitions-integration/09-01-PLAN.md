---
phase: 09-transitions-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ora/src/animation/mod.rs
  - ora/src/animation/tween.rs
  - ora/src/animation/easing.rs
  - ora/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Tween<T> can interpolate Color, f32, f64 values over time with configurable easing"
    - "Tween::retarget() starts new animation from current interpolated value without visual jump"
    - "Easing enum supports Linear, EaseIn, EaseOut, EaseInOut, Spring with correct cubic math"
    - "CubicBezier supports custom CSS-compatible bezier curves via Newton-Raphson solver"
  artifacts:
    - path: "ora/src/animation/mod.rs"
      provides: "Module declaration and re-exports"
      exports: ["Tween", "TweenState", "Tweenable", "Easing", "CubicBezier"]
    - path: "ora/src/animation/tween.rs"
      provides: "Tween<T> interpolation engine with retarget support"
      min_lines: 150
    - path: "ora/src/animation/easing.rs"
      provides: "Easing enum + CubicBezier + Spring + cubic math functions"
      min_lines: 100
  key_links:
    - from: "ora/src/animation/tween.rs"
      to: "ora/src/animation/easing.rs"
      via: "Easing::apply() used in Tween::update()"
      pattern: "self\\.easing\\.apply"
    - from: "ora/src/animation/tween.rs"
      to: "ora/src/style/mod.rs"
      via: "Tweenable impl for Color"
      pattern: "impl Tweenable for Color"
---

<objective>
Port the animation primitives (Tween<T>, Tweenable trait, Easing enum, CubicBezier) from wgpu_client to ora.

Purpose: These primitives are the foundation for the CSS-like transition system. They must exist in ora before TransitionSpec/TransitionState can be built.

Output: `ora/src/animation/` module with tween.rs, easing.rs, and mod.rs. All types exported from `ora::animation`.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-transitions-integration/09-RESEARCH.md

Source files to port from:
@wgpu_client/src/design_system/animation/tween.rs
@wgpu_client/src/design_system/animation/easing.rs
@wgpu_client/src/design_system/tokens/motion.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Easing enum and CubicBezier to ora</name>
  <files>ora/src/animation/easing.rs, ora/src/animation/mod.rs</files>
  <action>
  Create `ora/src/animation/easing.rs` by combining:
  1. The `Easing` enum from `wgpu_client/src/design_system/tokens/motion.rs` (Linear, EaseIn, EaseOut, EaseInOut, Spring variants with `apply()` method and cubic math functions)
  2. The `CubicBezier` struct from `wgpu_client/src/design_system/animation/easing.rs` (with Newton-Raphson solver, CSS preset methods: ease(), ease_in(), ease_out(), ease_in_out())
  3. The `Spring` struct from the same file (with gentle/wobbly/stiff presets and damped spring physics)

  Changes from wgpu_client version:
  - Remove `Duration` enum and `MotionPreference` — not needed in ora (duration is specified as u32 ms on TransitionConfig)
  - Easing enum is self-contained (no re-export from tokens)
  - Add `#[derive(Clone, Copy, Debug, Default, PartialEq, Eq, Hash)]` on Easing with `EaseOut` as default
  - Keep all existing tests, adapt imports

  Create `ora/src/animation/mod.rs` with:
  ```rust
  pub mod easing;
  pub mod tween;
  pub use easing::{Easing, CubicBezier, Spring};
  pub use tween::{Tween, TweenState, Tweenable};
  ```
  </action>
  <verify>`cargo test -p ora --lib animation::easing` passes (bezier and spring tests)</verify>
  <done>Easing, CubicBezier, Spring types compile and pass tests in ora crate</done>
</task>

<task type="auto">
  <name>Task 2: Port Tween and Tweenable to ora</name>
  <files>ora/src/animation/tween.rs, ora/src/lib.rs</files>
  <action>
  Create `ora/src/animation/tween.rs` by porting from `wgpu_client/src/design_system/animation/tween.rs`.

  Changes from wgpu_client version:
  - Change `use crate::design_system::tokens::Easing` to `use super::easing::Easing`
  - Change `use crate::theme::Color` to `use crate::style::Color`
  - The `Tweenable for Color` impl uses `crate::style::Color` (ora's Color type which has r/g/b/a as f32 fields — verify field names match)
  - Keep ALL existing Tweenable impls: f32, f64, i32, u8, (f32,f32), [f32;2], [f32;4], Color
  - Keep ALL methods: new, ease_out, instant, start, retarget, restart, update, value, value_owned, target, state, is_playing, is_complete, finish, reset, progress
  - Keep ALL tests, adapt Color import

  Update `ora/src/lib.rs`:
  - Add `pub mod animation;` declaration

  Verify ora's Color struct has r, g, b, a fields as f32. If Color::lerp signature differs from Tweenable::lerp (takes &Self vs Self), adapt accordingly. Check: in wgpu_client, Color::lerp in test takes `a.lerp(b, 0.5)` with value (not reference) — the Tweenable trait uses `&self` and `&Self` so the test may need `a.lerp(&b, 0.5)`.
  </action>
  <verify>`cargo test -p ora --lib animation::tween` passes AND `cargo check -p ora` compiles clean</verify>
  <done>Tween<T> with all Tweenable impls compiles and tests pass. `ora::animation` is publicly accessible.</done>
</task>

</tasks>

<verification>
- `cargo test -p ora --lib animation` — all animation tests pass
- `cargo check -p ora` — no compilation errors
- Verify exports: `Tween`, `TweenState`, `Tweenable`, `Easing`, `CubicBezier`, `Spring` all accessible from `ora::animation`
</verification>

<success_criteria>
- Tween<Color> can interpolate between two colors over a duration with EaseOut easing
- Tween::retarget() smoothly continues from current value to new target
- CubicBezier::ease_out().apply(0.5) returns a value between 0 and 1
- All animation tests pass (both ported and existing)
</success_criteria>

<output>
After completion, create `.planning/phases/09-transitions-integration/09-01-SUMMARY.md`
</output>
