---
phase: 09-transitions-integration
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - ora/src/views/tab_bar.rs
  - ora/src/views/status_bar.rs
  - ora/src/views/gutter.rs
  - ora/src/views/sidebar.rs
  - ora/src/views/dialog.rs
  - ora/src/views/file_tree.rs
  - ora/src/views/text_area.rs
  - ora/src/views/app_layout.rs
  - ora/src/views/command_palette.rs
  - ora/src/views/panel_manager.rs
  - ora/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "No ora view file contains 'use core_editor' imports"
    - "All views use ora::editor_adapter types for presentation data"
    - "ora crate compiles without core_editor dependency (or core_editor is dev-dependency only)"
  artifacts:
    - path: "ora/src/views/tab_bar.rs"
      provides: "TabBarView using ora mirror types"
      contains: "use crate::editor_adapter"
    - path: "ora/src/views/app_layout.rs"
      provides: "AppLayout accepting EditorDataSource"
      contains: "EditorDataSource"
  key_links:
    - from: "ora/src/views/tab_bar.rs"
      to: "ora/src/editor_adapter/types.rs"
      via: "import of TabBarPresentation"
      pattern: "use crate::editor_adapter"
    - from: "ora/src/views/app_layout.rs"
      to: "ora/src/editor_adapter/mod.rs"
      via: "EditorDataSource trait usage"
      pattern: "EditorDataSource"
---

<objective>
Migrate all ora views from direct core_editor imports to ora's mirror types, and remove core_editor dependency from ora's Cargo.toml.

Purpose: This enforces the adapter boundary — ora views only know about ora types, not core_editor internals. The core_editor dependency moves entirely to wgpu_client's adapter implementation.

Output: All view files updated, core_editor removed from ora/Cargo.toml (or moved to dev-dependencies if needed for tests).
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-transitions-integration/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace core_editor imports in all view files</name>
  <files>ora/src/views/tab_bar.rs, ora/src/views/status_bar.rs, ora/src/views/gutter.rs, ora/src/views/sidebar.rs, ora/src/views/dialog.rs, ora/src/views/file_tree.rs, ora/src/views/text_area.rs, ora/src/views/command_palette.rs, ora/src/views/panel_manager.rs</files>
  <action>
  For each view file that imports from core_editor (identified by `grep -r "use core_editor" ora/src/views/`):

  1. Replace `use core_editor::view_model::{TypeA, TypeB}` with `use crate::editor_adapter::{TypeA, TypeB}`
  2. This is a mechanical find-and-replace — the mirror types have identical names and field structures

  Files to update (from research):
  - `tab_bar.rs`: TabBarPresentation, TabPresentation
  - `status_bar.rs`: StatusPresentation
  - `gutter.rs`: GutterModel, LinePresentation
  - `sidebar.rs`: SidebarPresentation, FileTreePresentation
  - `dialog.rs`: DialogPresentation
  - `file_tree.rs`: FileTreePresentation, FileTreeNode
  - `text_area.rs`: CaretPresentation, LinePresentation, RenderModel, TextStyle
  - `command_palette.rs`: check if it imports core_editor (may not)
  - `panel_manager.rs`: check if it imports core_editor (may not)

  Also update test modules (`#[cfg(test)]`) in each file — they also import core_editor types.

  IMPORTANT: If any view uses a method or trait impl from core_editor types that isn't on the mirror type, add that method to the mirror type in `ora/src/editor_adapter/types.rs`. The mirror types should have all constructors and utility methods the views need.
  </action>
  <verify>`grep -r "use core_editor" ora/src/views/` returns zero results AND `cargo check -p ora` compiles</verify>
  <done>All ora view files use crate::editor_adapter types exclusively, zero core_editor imports remain</done>
</task>

<task type="auto">
  <name>Task 2: Update AppLayout to accept EditorDataSource + remove core_editor dep</name>
  <files>ora/src/views/app_layout.rs, ora/Cargo.toml</files>
  <action>
  1. In `app_layout.rs`, update the struct to hold presentation data from EditorDataSource rather than directly from core_editor. The AppLayout currently receives RenderModel in its render() method — ensure it uses `crate::editor_adapter::RenderModel`.

  2. Remove `core_editor` from `ora/Cargo.toml` dependencies entirely. If tests in ora need core_editor types for test data construction, move it to `[dev-dependencies]`. But ideally tests should construct mirror types directly (they're self-contained).

  3. Run `cargo check -p ora` to verify the crate compiles without core_editor as a regular dependency.

  4. If any compilation errors remain from other files in ora/src/ that import core_editor, fix those too.

  5. Run `cargo check --workspace` to ensure wgpu_client still compiles (it imports ora and core_editor independently).
  </action>
  <verify>`cargo check --workspace` compiles. `grep "core_editor" ora/Cargo.toml` shows only dev-dependencies (or nothing).</verify>
  <done>ora crate has no runtime dependency on core_editor. All workspace crates compile.</done>
</task>

</tasks>

<verification>
- `grep -r "use core_editor" ora/src/views/` returns zero results
- `grep -r "use core_editor" ora/src/` returns zero results (except possibly dev test helpers)
- `cargo check --workspace` compiles clean
- `cargo test -p ora` passes
</verification>

<success_criteria>
- Every ora view imports from crate::editor_adapter, never from core_editor
- ora/Cargo.toml has no [dependencies] entry for core_editor (dev-dependencies OK)
- Full workspace compiles and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-transitions-integration/09-03-SUMMARY.md`
</output>
