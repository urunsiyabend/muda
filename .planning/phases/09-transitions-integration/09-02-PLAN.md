---
phase: 09-transitions-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ora/src/editor_adapter/mod.rs
  - ora/src/editor_adapter/types.rs
  - ora/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "ora defines an EditorDataSource trait that abstracts over any editor backend"
    - "ora defines mirror types for all presentation structs (TabBarPresentation, StatusPresentation, etc.)"
    - "ora does NOT import core_editor types in the editor_adapter module"
  artifacts:
    - path: "ora/src/editor_adapter/mod.rs"
      provides: "EditorDataSource trait + re-exports of mirror types"
      exports: ["EditorDataSource", "EditorCommand"]
    - path: "ora/src/editor_adapter/types.rs"
      provides: "Mirror presentation types matching core_editor::view_model structs"
      contains: "pub struct RenderModel"
  key_links:
    - from: "ora/src/editor_adapter/types.rs"
      to: "core_editor/src/view_model/mod.rs"
      via: "Structural mirror (same field names and types)"
      pattern: "pub struct RenderModel"
---

<objective>
Create the EditorDataSource trait and mirror presentation types in ora, establishing the adapter boundary between ora and core_editor.

Purpose: The locked decision says ora views must NOT import core_editor types directly. This plan creates the ora-side types and trait so views can be migrated in the next plan.

Output: `ora/src/editor_adapter/` module with trait definition and all mirror types.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-transitions-integration/09-RESEARCH.md

Types to mirror:
@core_editor/src/view_model/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mirror presentation types in ora</name>
  <files>ora/src/editor_adapter/types.rs</files>
  <action>
  Create `ora/src/editor_adapter/types.rs` that mirrors ALL public types from `core_editor/src/view_model/mod.rs`:

  Mirror these types with identical field names and types:
  - `TextStyle` enum (Normal, Selection, Keyword, String, Number, Comment, Type, Function, Variable, Operator, Punctuation, Constant, Module, Attribute, Macro, LineNumber, CurrentLineNumber, Error, Warning)
  - `VisualPosition` struct (row: usize, column: usize)
  - `StyledSpan` struct (text: String, style: TextStyle) with `new()`, `raw()`, `selection()` constructors
  - `LinePresentation` struct (line_number, is_current_line, spans)
  - `GutterModel` struct (width, visible, total_lines) with `new()`, `line_number_width()`
  - `StatusPresentation` struct (title, dirty, cursor_line, cursor_column, language, total_lines, message)
  - `CaretPresentation` struct (position: VisualPosition, visible: bool)
  - `DialogPresentation` enum (None, UnsavedChangesConfirmation { action_description, unsaved_documents })
  - `FileEntryPresentation` struct (name, is_dir, is_selected)
  - `SidebarPresentation` struct (visible, focused, directory_name, entries, width)
  - `TabPresentation` struct (view_id: u64, title, is_active, is_dirty)
  - `FileTreeNode` struct (name, extension, is_dir, is_expanded, children) with `file()`, `dir()` constructors
  - `FileTreePresentation` struct (roots, selected_index)
  - `TabBarPresentation` struct (tabs, visible)
  - `RenderModel` struct (visible_lines, gutter, caret, status, tab_bar, dialog, sidebar, scroll_x, scroll_y) with `display_title()`

  All types must derive `Clone, Debug` at minimum. Simple value types also derive `Default`.

  IMPORTANT: Do NOT add `core_editor` as a dependency. These are standalone mirror types that happen to have the same structure. The `From` conversions will be implemented in wgpu_client, not here.

  Also define `EditorCommand` enum in this file with variants matching the commands ora needs to dispatch to the editor. Read `wgpu_client/src/input.rs` to identify the command variants (MoveCursor, InsertChar, DeleteBackward, DeleteForward, NewLine, Save, Undo, Redo, SelectAll, Copy, Cut, Paste, etc.). If unsure, include a reasonable set — can be extended later.
  </action>
  <verify>`cargo check -p ora` compiles clean with no core_editor imports in this file</verify>
  <done>All presentation types defined in ora with zero dependency on core_editor</done>
</task>

<task type="auto">
  <name>Task 2: Create EditorDataSource trait and module exports</name>
  <files>ora/src/editor_adapter/mod.rs, ora/src/lib.rs</files>
  <action>
  Create `ora/src/editor_adapter/mod.rs`:

  ```rust
  pub mod types;
  pub use types::*;

  /// Trait abstracting over an editor backend.
  /// wgpu_client implements this for core_editor::app::App.
  /// ora views receive &dyn EditorDataSource to query presentation data.
  pub trait EditorDataSource {
      /// Build the complete render model for the current frame.
      fn build_render_model(&self, viewport_lines: usize) -> RenderModel;

      /// Dispatch an editor command (from keyboard input or UI action).
      fn dispatch_command(&mut self, cmd: EditorCommand);

      /// Get the window title (for title bar updates).
      fn window_title(&self) -> String;
  }
  ```

  Update `ora/src/lib.rs`:
  - Add `pub mod editor_adapter;`

  Verify that `ora` crate compiles without any new dependencies on core_editor.
  </action>
  <verify>`cargo check -p ora` compiles. `grep -r "core_editor" ora/src/editor_adapter/` returns no results.</verify>
  <done>EditorDataSource trait is public at `ora::editor_adapter::EditorDataSource`. Mirror types accessible at `ora::editor_adapter::RenderModel` etc. No core_editor dependency in this module.</done>
</task>

</tasks>

<verification>
- `cargo check -p ora` compiles clean
- `grep -r "core_editor" ora/src/editor_adapter/` returns zero matches
- All presentation types from core_editor::view_model are mirrored in ora::editor_adapter::types
- EditorDataSource trait has build_render_model, dispatch_command, window_title methods
</verification>

<success_criteria>
- ora::editor_adapter module exists with EditorDataSource trait and all mirror types
- Zero core_editor imports in the new module
- Types structurally match core_editor's view_model types (same field names/types)
</success_criteria>

<output>
After completion, create `.planning/phases/09-transitions-integration/09-02-SUMMARY.md`
</output>
