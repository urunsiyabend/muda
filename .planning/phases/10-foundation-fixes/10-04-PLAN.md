---
phase: 10-foundation-fixes
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All five FIX requirements verified working together in the running editor"
    - "No regressions in existing editor functionality"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 10 fixes work together in the running editor.

Purpose: Each plan was developed independently. This checkpoint verifies the
combined result: event-driven redraw + caret blink + v2 commands + trait split +
selection rendering all function correctly in a single build.

Output: Verified working editor with all Phase 10 requirements met.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-foundation-fixes/10-01-SUMMARY.md
@.planning/phases/10-foundation-fixes/10-02-SUMMARY.md
@.planning/phases/10-foundation-fixes/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run automated checks</name>
  <files></files>
  <action>
  Run the full verification suite:
  1. `cargo check -p ora -p wgpu_client` — no compilation errors
  2. `cargo test -p ora --lib` — all tests pass
  3. `cargo test -p wgpu_client --lib` — all tests pass (if any exist)
  4. `cargo clippy -p ora -p wgpu_client` — no warnings (or only pre-existing ones)

  Also fix the known issue: `Document::title()` returns "Yeni Dosya" (Turkish) instead of
  "[New File]". Search for "Yeni Dosya" in the codebase and replace with "[New File]" or
  "Untitled". This is a small fix that belongs in Phase 10 per STATE.md known issues.
  </action>
  <verify>All cargo commands pass with zero errors.</verify>
  <done>Full build passes. Known "Yeni Dosya" issue fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  All Phase 10 Foundation Fixes:
  - FIX-01: Event-driven redraw (no idle GPU waste)
  - FIX-02: Selection rendering (visible highlights)
  - FIX-03: v2 command stubs (Ctrl+W, Ctrl+F, etc.)
  - FIX-04: EditorDataSource trait split
  - FIX-05: Caret blink via timer (WaitUntil)
  </what-built>
  <how-to-verify>
  Run `cargo run -p wgpu_client` and test:

  1. **Idle GPU (FIX-01):** Open Task Manager (Ctrl+Shift+Esc) -> Performance -> GPU.
     Let the editor sit idle for 5 seconds. GPU usage should be near 0% (small spikes
     every 500ms for caret blink are expected).

  2. **Caret blink (FIX-05):** Watch the caret. After ~1 second of no typing, it should
     blink (visible/invisible snap). Type a character — caret goes solid immediately.
     Stop typing — after ~1 second, blinking resumes.

  3. **Selection (FIX-02):** Click into the text area. Press Shift+Right arrow 5-10 times.
     A blue highlight should appear behind the selected characters. The text should still
     be visible (not disappear). Press Shift+Down to extend selection to the next line —
     both lines should show highlights.

  4. **v2 commands (FIX-03):**
     - Press Ctrl+W — status bar shows "Close Tab: not yet available"
     - Press Ctrl+F — status bar shows "Find: not yet available"
     - Press Ctrl+N — status bar shows "New: not yet available"
     - Press Ctrl+O — status bar shows "Open File: not yet available"
     - Press Ctrl+Shift+S — status bar shows "Save As: not yet available"
     - Press Ctrl+S — normal save (no "not yet available" message)

  5. **No regressions:**
     - Type characters — they appear correctly
     - Arrow keys move cursor
     - Backspace deletes
     - Mouse hover on tabs shows hover effects
     - Scroll wheel scrolls smoothly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All five FIX requirements verified in a single running build.
No regressions in existing editing, navigation, or visual functionality.
</verification>

<success_criteria>
- User confirms all 5 verification items pass
- No regressions identified
- Phase 10 ready for completion
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-fixes/10-04-SUMMARY.md`
</output>
