---
phase: 14-file-browser
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - ora/src/editor_adapter/types.rs
  - ora/src/editor_adapter/mod.rs
  - ora/src/platform/event_loop.rs
  - wgpu_client/src/adapter.rs
  - ora/src/views/sidebar.rs
autonomous: true

must_haves:
  truths:
    - "User can open a folder via keybinding or command palette to set a new workspace root"
    - "Opening a new folder refreshes the sidebar tree and persists the workspace path"
    - "Sidebar header shows the workspace root folder name instead of EXPLORER"
    - "Empty sidebar shows No folder open message with an Open Folder button"
  artifacts:
    - path: "ora/src/editor_adapter/types.rs"
      provides: "PendingFileOp::OpenFolder variant"
      contains: "OpenFolder"
    - path: "ora/src/platform/event_loop.rs"
      provides: "spawn_open_folder_dialog async future"
      contains: "spawn_open_folder_dialog"
    - path: "wgpu_client/src/adapter.rs"
      provides: "handle_folder_opened callback"
      contains: "handle_folder_opened"
    - path: "ora/src/views/sidebar.rs"
      provides: "Sidebar header with workspace name + empty state Open Folder button"
      contains: "No folder open"
  key_links:
    - from: "ora/src/platform/event_loop.rs"
      to: "wgpu_client/src/adapter.rs"
      via: "handle_folder_opened callback after pick_folder dialog"
      pattern: "handle_folder_opened"
    - from: "wgpu_client/src/adapter.rs"
      to: "core_editor/src/view/sidebar.rs"
      via: "set_base_directory + auto_expand_first_level on folder open"
      pattern: "set_base_directory"
    - from: "ora/src/views/sidebar.rs"
      to: "ora/src/editor_adapter/types.rs"
      via: "dispatch OpenFolder command from empty state button"
      pattern: "OpenFolder"
---

<objective>
Add Open Folder dialog command and improve sidebar chrome (header title, empty state).

Purpose: Users need to set a workspace root via dialog, and the sidebar should show the workspace name in its header. When no folder is open, the sidebar should guide the user with an Open Folder button.

Output: Working Open Folder keybinding/command, sidebar header with workspace name, empty state with button.
</objective>

<execution_context>
@C:\Users\uruns\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\uruns\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-file-browser/14-CONTEXT.md
@.planning/phases/14-file-browser/14-RESEARCH.md
@.planning/phases/14-file-browser/14-01-SUMMARY.md
@ora/src/editor_adapter/types.rs
@ora/src/editor_adapter/mod.rs
@ora/src/platform/event_loop.rs
@wgpu_client/src/adapter.rs
@ora/src/views/sidebar.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenFolder command + PendingFileOp variant + dialog</name>
  <files>ora/src/editor_adapter/types.rs, ora/src/editor_adapter/mod.rs, ora/src/platform/event_loop.rs, wgpu_client/src/adapter.rs</files>
  <action>
  **In ora/src/editor_adapter/types.rs:**
  1. Add `OpenFolder` variant to `PendingFileOp` enum.
  2. Add `OpenFolder` variant to `EditorCommand` enum (no parameters).

  **In ora/src/editor_adapter/mod.rs:**
  3. Add method to `FileOpDataSource` trait:
     ```rust
     fn handle_folder_opened(&mut self, path: PathBuf);
     ```
     This is called by the event loop after the user picks a folder.

  **In wgpu_client/src/adapter.rs:**
  4. In `CommandDispatcher::dispatch_command`, handle `EditorCommand::OpenFolder`:
     - If `!self.dialog_open`, set `self.pending_file_op = Some(PendingFileOp::OpenFolder)`.

  5. Implement `handle_folder_opened(&mut self, path: PathBuf)` in the `FileOpDataSource` impl:
     - Canonicalize the path via `std::fs::canonicalize`.
     - Call `self.app.sidebar.set_base_directory(canonical_path.clone())` — this already calls auto_expand_first_level (from Plan 01).
     - Set `self.app.sidebar.show()` to ensure sidebar is visible.
     - Set `self.workspace_path = Some(canonical_path.clone())`.
     - Call `save_state()` to persist the new workspace path.
     - Set `self.app.needs_render = true`.
     - TODO for Plan 04: close existing tabs (prompt if dirty). For now, just switch workspace without closing tabs.

  6. Add `EditorCommand::OpenFolder` to the keybinding system. Use Ctrl+K followed by Ctrl+O (VS Code style two-chord), OR simpler: just add it as a command palette entry. Since two-chord keybindings may be complex, bind it to Ctrl+Shift+O (distinct from Ctrl+O which opens a file). Register this in the keybinding translation in `ora/src/events/editor_input.rs`. Check the existing `translate_editor_command` function and add the Ctrl+Shift+O mapping.

  **In ora/src/platform/event_loop.rs:**
  7. Add `PendingFileOp::OpenFolder` match arm in `poll_pending_file_ops()`:
     ```rust
     PendingFileOp::OpenFolder => self.spawn_open_folder_dialog(),
     ```

  8. Implement `spawn_open_folder_dialog(&mut self)`:
     ```rust
     fn spawn_open_folder_dialog(&mut self) {
         let adapter = Rc::clone(self.editor_adapter.as_ref().unwrap());
         adapter.borrow_mut().set_dialog_open(true);
         let window = self.gpu_state.as_ref().unwrap().window.clone();

         self.app_context.spawn(async move {
             let handle = rfd::AsyncFileDialog::new()
                 .set_title("Open Folder")
                 .pick_folder()
                 .await;

             if let Some(handle) = handle {
                 let path = handle.path().to_path_buf();
                 adapter.borrow_mut().handle_folder_opened(path);
             }
             adapter.borrow_mut().set_dialog_open(false);
             window.request_redraw();
         }).detach();
     }
     ```

  9. Add `EditorCommand::OpenFolder` to the command palette registry. Find the command palette's command list (likely in `ora/src/views/command_palette.rs` or the adapter) and add "Open Folder" with the OpenFolder command.
  </action>
  <verify>
  `cargo build -p wgpu_client` compiles. Run the editor, press Ctrl+Shift+O (or use command palette "Open Folder"), pick a directory — sidebar should refresh to show that directory's contents with first-level expanded. state.json should update with the new workspace_path.
  </verify>
  <done>Open Folder dialog works via keybinding and command palette. Sidebar refreshes to new workspace. Workspace path persisted.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar header with workspace name + empty state Open Folder button</name>
  <files>ora/src/views/sidebar.rs</files>
  <action>
  1. In `SidebarView::render()` (or the header rendering section), replace the hardcoded "EXPLORER" title with the `directory_name` field from `SidebarPresentation`. The field already contains the workspace root folder name (set by `ViewModelBuilder` from `sidebar.base_directory`). When `directory_name` is empty or "Files" (the default when no folder is open), show "EXPLORER" as fallback.

  2. In the sidebar content area, when `SidebarPresentation.tree` is empty AND `directory_name` equals "Files" or is the default (indicating no workspace open):
     - Render a centered "No folder open" text message.
     - Render a clickable "Open Folder" button below it.
     - The button's click handler should dispatch `EditorCommand::OpenFolder` through the sidebar's dispatch callback. Follow the same pattern used for `OpenSidebarFile` / `ToggleSidebarDir` dispatching in the existing SidebarView.

  3. Style the empty state: use muted foreground color for the text, standard button styling for the Open Folder button. Keep it simple — no elaborate empty state art.

  4. Verify the `SidebarPresentation.directory_name` is correctly populated. Check `wgpu_client/src/adapter.rs` `build_render_model` or `build_sidebar_presentation` — the directory_name should already come from `sidebar.base_directory.file_name()`. If it says "Files" when a workspace is open, there may be a bug to fix in the presentation builder.
  </action>
  <verify>
  `cargo build -p wgpu_client` compiles. Run with no args and no previous workspace — sidebar should show "EXPLORER" header and "No folder open" + button. Click button or Ctrl+Shift+O, pick folder — header changes to folder name, tree appears.
  </verify>
  <done>Sidebar header shows workspace folder name. Empty state shows "No folder open" with functional Open Folder button.</done>
</task>

</tasks>

<verification>
1. `cargo build -p wgpu_client` — clean compile
2. Open Folder via Ctrl+Shift+O opens native folder picker
3. Picking a folder sets workspace root, sidebar refreshes
4. Sidebar header shows folder name (e.g., "muda") instead of "EXPLORER"
5. Empty sidebar (no workspace) shows "No folder open" + Open Folder button
6. Clicking the Open Folder button in empty state opens folder picker
7. Command palette includes "Open Folder" entry
</verification>

<success_criteria>
- Open Folder dialog fully functional via keybinding and command palette
- Sidebar header reflects workspace root name
- Empty state guides user to open a folder
- Workspace path persisted to state.json on folder open
</success_criteria>

<output>
After completion, create `.planning/phases/14-file-browser/14-02-SUMMARY.md`
</output>
