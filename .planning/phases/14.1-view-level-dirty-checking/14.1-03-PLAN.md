---
phase: 14.1-view-level-dirty-checking
plan: 03
type: execute
wave: 3
depends_on: ["14.1-02"]
files_modified:
  - ora/src/platform/event_loop.rs
autonomous: false

must_haves:
  truths:
    - "Frame time during scroll stays under 2ms in release mode with 10 tabs open"
    - "Layout cache hit rate above 90% during continuous editor scroll"
    - "Non-scrolling views (tab bar, status bar) are not re-rendered during editor scroll (verified by dirty flag state)"
    - "Sidebar scroll with viewport virtualization works correctly"
  artifacts:
    - path: "ora/src/platform/event_loop.rs"
      provides: "Final tuned event loop with FrameDirtyFlags and conditional layout"
  key_links:
    - from: "scroll event"
      to: "cached_layout_outputs"
      via: "needs_layout stays false on editor scroll → compute_flexbox skipped"
      pattern: "run_full_layout.*false"
---

<objective>
Verify all Phase 14.1 success criteria with measured benchmarks and human visual verification.

Purpose: Confirm that the paint-phase scroll offset (Plan 01) and conditional layout (Plan 02) work together to meet all 5 success criteria from the roadmap. This is the final gate before marking Phase 14.1 complete.

Output: Verified measurements and visual confirmation of correct behavior.
</objective>

<execution_context>
@C:\Users\uruns\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\uruns\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-view-level-dirty-checking/14.1-RESEARCH.md
@.planning/phases/14.1-view-level-dirty-checking/14.1-01-SUMMARY.md
@.planning/phases/14.1-view-level-dirty-checking/14.1-02-SUMMARY.md
@ora/src/platform/event_loop.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark scroll performance with 10 tabs</name>
  <files>ora/src/platform/event_loop.rs</files>
  <action>
Run the editor in release mode with --show-fps. Open 10 files (use Ctrl+O or open from sidebar). With all 10 tabs open and sidebar visible:

1. Scroll continuously through a large file (100+ lines) for 5-10 seconds
2. Record from --show-fps output:
   - Average frame time during scroll
   - Max frame time during scroll
   - Layout cache hit rate (layout: XX%)
   - Glyph cache hit rate

3. Compare against success criteria:
   - SC1: Layout cache hit rate > 90% during scroll
   - SC3: visible_lines count stable (no debug_assert failures)
   - SC5: Frame time under 2ms in release mode

4. Also test:
   - SC2: Switch to a non-scrolling action (just hover over tab bar) — observe that layout is NOT triggered (cache hit rate stays high)
   - SC4: Scroll sidebar with many files — should work correctly, layout IS triggered for sidebar scroll

If frame time is above 2ms during scroll, investigate:
- Check if compute_flexbox is being called (layout_misses count should not increase during scroll)
- Check if glyph cache is being thrashed (glyph hit rate should stay high)
- If layout misses occur, check the debug_assert — element count may be changing

If any issues found, add targeted logging and fix before proceeding to human verification.
  </action>
  <verify>
--show-fps shows:
- Layout hit rate 90%+ during continuous editor scroll
- Frame time under 2ms in release mode with 10 tabs open
- No debug_assert failures (element count stable during scroll)
  </verify>
  <done>Benchmark numbers captured: frame time under 2ms, layout hit rate above 90% during scroll</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Paint-phase scroll offset and conditional layout caching for editor scroll.

Changes made across Plans 01-02:
- Replaced mt(scroll_shift) with push_offset() in TextAreaView (4 locations) and GutterView (1 location)
- Made editor scroll NOT set needs_layout = true (only sidebar scroll does)
- Added FrameDirtyFlags tracking which UI regions changed
- Added debug_assert for element count stability during paint-only frames
  </what-built>
  <how-to-verify>
Run: `cargo run --release -- --show-fps` from the wgpu_client directory.

1. **Editor scroll smoothness:**
   - Open a file with 100+ lines
   - Scroll up and down with mouse wheel
   - Text should scroll smoothly with no visual glitches
   - Line numbers in gutter should stay aligned with text
   - Caret position should stay correct
   - Current-line highlight should track correctly
   - Selection backgrounds should stay aligned if any selection exists

2. **Layout cache during scroll:**
   - Watch --show-fps layout hit rate
   - During continuous scroll, layout hit rate should be 90%+
   - Frame time should be under 2ms

3. **Sidebar scroll:**
   - Open a workspace with many files
   - Scroll the sidebar — items should scroll correctly
   - No missing items, no misaligned text

4. **Non-scroll operations still work:**
   - Type some text — renders correctly
   - Switch tabs — content updates
   - Resize window — layout adjusts
   - Open/close sidebar — toggles correctly

5. **Edge cases:**
   - Scroll to top of file — no overshoot or visual artifacts
   - Scroll to bottom of file — last line visible
   - Fast scroll (rapid wheel) — no corruption
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 14.1 Success Criteria (from roadmap):
1. [x] Scrolling does NOT trigger full layout — layout cache hit rate above 90%
2. [x] Non-scrolling views replay cached state during editor scroll (verified via FrameDirtyFlags)
3. [x] Editor scroll uses paint-phase coordinate offset — visible_lines count stable, LayoutIds stable
4. [x] Sidebar scroll with viewport virtualization still works correctly
5. [x] Frame time during scroll under 2ms in release mode with 10 tabs open
</verification>

<success_criteria>
- All 5 roadmap success criteria verified with measurements
- Human visual verification approved
- No regressions in typing, tab switching, sidebar, or other non-scroll operations
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-view-level-dirty-checking/14.1-03-SUMMARY.md`
</output>
