[workspace]
members = ["core_editor", "ratatui_client", "wgpu_client", "ora"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"

# Keep benchmarks at workspace level for now
# They will need to be updated to use core_editor
